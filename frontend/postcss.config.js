/**
 * PostCSS configuration.
 *
 * Enables Tailwind CSS and autoprefixer in the Vite build pipeline.
 * No custom PostCSS plugins are used beyond these two.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
